fastapi>=0.100.0  # 支持 pydantic v2
uvicorn>=0.24.0
pydantic>=2.7.4  # LangGraph 1.0.5+ 需要 pydantic v2
pydantic-settings>=2.0.0  # pydantic v2 的 BaseSettings
python-dotenv==1.0.0
httpx>=0.25.1  # 支持异步请求
python-jose==3.3.0
python-multipart==0.0.6
loguru==0.7.2
aiosqlite==0.19.0
openai>=1.0.0
aiomysql==0.2.0
redis==5.0.1
cachetools==5.3.2
cryptography>=3.4.8  # MySQL认证所需

# LangGraph 相关依赖（最新版本）
langgraph>=1.0.5  # LangGraph 核心库
langchain-core>=1.1.3  # LangChain 核心库（LangGraph 需要）
langchain>=1.1.3  # LangChain 完整库

# 注意：此版本使用最新版本的依赖
# - fastapi 0.100+: 支持 pydantic v2
# - pydantic v2: LangGraph 1.0.5+ 需要 pydantic v2
# - pydantic-settings: pydantic v2 的 BaseSettings 需要单独安装
# - python-jose: 不使用cryptography扩展，使用纯Python实现
# 
# 性能说明：
# - 此配置适合开发和测试环境
# - 生产环境建议使用带Rust优化的版本以获得更好性能

